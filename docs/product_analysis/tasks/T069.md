# T069 -- Refatorar Arquivos Existentes para Padrão do Project Rules

## 📋 Descrição

Refatorar todos os arquivos existentes no projeto que estão fora do padrão definido em `docs/project_rules/`, garantindo consistência de nomenclatura, estrutura e boas práticas.

## 🎯 Objetivo

Aplicar as convenções de nomenclatura e estrutura definidas nas regras do projeto a todos os arquivos existentes, eliminando inconsistências e preparando o codebase para receber novas funcionalidades.

## 📐 Regras Aplicáveis

### Backend (`docs/project_rules/backend.md`)

#### Nomenclatura de Arquivos (Seção 02)
- **CamelCase para classes**: arquivos devem ter nome igual ao da classe em CamelCase
  - ❌ `new_account_controller.ts` → ✅ `NewAccountController.ts`
  - ❌ `session_controller.ts` → ✅ `SessionController.ts`
  - ❌ `user.ts` → ✅ `User.ts`
  - ❌ `auth_middleware.ts` → ✅ `AuthMiddleware.ts`
  - ❌ `container_bindings_middleware.ts` → ✅ `ContainerBindingsMiddleware.ts`
  - ❌ `guest_middleware.ts` → ✅ `GuestMiddleware.ts`
  - ❌ `silent_auth_middleware.ts` → ✅ `SilentAuthMiddleware.ts`

#### Controllers (Seção 02)
- Devem conter somente: leitura de request, criação de DTOs, chamada de services, escrita de response
- Não devem conter regra de negócio
- Devem usar validação dos services, não diretamente

#### Services (Seção 02)
- Devem retornar resultados tipados (nunca `any`)
- Devem ter controle transacional quando modificarem dados

#### Models (Seção 02)
- Devem conter apenas: definições de colunas, relacionamentos, getters computados, mixins
- Evitar lógica de negócio
- Usar timestamps
- Utilizar soft-deletes

#### Validações (Seção 02)
- Devem ser chamadas dentro dos services, não nas controllers

#### Logging (Seção 01)
- Formato: `Classe.metodo - mensagem`
- Usar níveis apropriados (error, warn, info, debug)

### Edge Templates (`docs/project_rules/edge_templates.md`)
- Usar `@layout()` ao invés de layouts manuais
- Usar `@if(auth.user)` ao invés de `@loggedIn`/`@guest`
- Formulários com `{{ csrfField() }}`
- Rotas com `route('nome')`
- Flash messages com Alpine.js

## 🔍 Arquivos a Refatorar

### Controllers
- [ ] `app/controllers/new_account_controller.ts` → `NewAccountController.ts`
- [ ] `app/controllers/session_controller.ts` → `SessionController.ts`

### Models
- [ ] `app/models/user.ts` → `User.ts`
  - Verificar: timestamps, soft-deletes, sem lógica de negócio

### Middleware
- [ ] `app/middleware/auth_middleware.ts` → `AuthMiddleware.ts`
- [ ] `app/middleware/container_bindings_middleware.ts` → `ContainerBindingsMiddleware.ts`
- [ ] `app/middleware/guest_middleware.ts` → `GuestMiddleware.ts`
- [ ] `app/middleware/silent_auth_middleware.ts` → `SilentAuthMiddleware.ts`

### Validators
- [ ] `app/validators/user.ts` → revisar nomenclatura (ex.: `CreateUserValidator.ts`, `UpdateUserValidator.ts`)

### Exceptions
- [ ] `app/exceptions/handler.ts` → revisar padrão de logging

### Database
- [ ] `database/migrations/1758943358073_create_users_table.ts` → verificar: timestamps, soft-deletes

### Views (Edge Templates)
- [ ] `resources/views/pages/auth/login.edge` → verificar: `@layout()`, `@if(auth.user)`, `csrfField()`, `route()`
- [ ] `resources/views/pages/auth/signup.edge` → verificar: `@layout()`, `@if(auth.user)`, `csrfField()`, `route()`
- [ ] `resources/views/pages/errors/not_found.edge` → verificar padrão
- [ ] `resources/views/pages/errors/server_error.edge` → verificar padrão
- [ ] `resources/views/partials/flash_alerts.edge` → verificar Alpine.js integration
- [ ] Todos componentes em `resources/views/components/` → verificar padrões

### Config
- [ ] Revisar todas as configs em `config/` para conformidade com regras

### Routes
- [ ] `start/routes.ts` → verificar: rotas nomeadas com `.as()`, middleware groups, import lazy

## ✅ Checklist de Validação

Após refatoração de cada arquivo:
- [ ] Nomenclatura correta (CamelCase)
- [ ] Imports atualizados em todos os arquivos dependentes
- [ ] Testes passando (quando existirem)
- [ ] Build sem erros (`npm run build`)
- [ ] Linting passando (`npm run lint`)

## 🚀 Passos de Execução

1. **Renomear arquivos** seguindo CamelCase
2. **Atualizar imports** em todos os arquivos que referenciam os arquivos renomeados
3. **Revisar código** de cada arquivo aplicando as regras específicas
4. **Atualizar rotas** em `start/routes.ts` para apontar para controllers renomeados
5. **Atualizar kernel** em `start/kernel.ts` para middlewares renomeados
6. **Executar build e lint** para verificar erros
7. **Executar testes** existentes

## ⚠️ Atenção

- **Sempre atualizar imports** após renomear arquivos
- **Manter funcionalidade existente** - refatoração não deve quebrar features
- **Testar manualmente** após refatoração (login, signup, etc.)

## 📚 Referências

- `docs/project_rules/index.md`
- `docs/project_rules/backend.md`
- `docs/project_rules/edge_templates.md`
- `docs/project_rules/frontend/` (todos os arquivos)
